{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "ionic build --prod"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/www/$1"
    }
  ]
}